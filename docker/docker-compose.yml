version: '3.8'

services:
  optix-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ../data:/app/data
      - ../models:/app/models
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
